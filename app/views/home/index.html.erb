<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Input Length Checker</title>
    <style>
      body {
        font-family: Arial, sans-serif;
        background-color: #f9f9f9;
        margin: 0;
        padding: 0;
      }
      .container {
        max-width: 600px;
        margin: 0 auto;
        padding: 20px;
        background-color: #fff;
        box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.1);
        position: absolute;
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
      }
      h1 {
        font-size: 32px;
        margin: 0 0 20px;
        color: #333;
        text-align: center;
      }
      label {
        display: block;
        margin: 0 0 10px;
        font-size: 18px;
        color: #333;
      }
      input[type="text"] {
        display: block;
        width: 100%;
        padding: 10px;
        font-size: 16px;
        border: none;
        border-radius: 4px;
        box-shadow: 0px 0px 5px rgba(0, 0, 0, 0.1);
        background-color: #f5f5f5;
        color: #333;
      }
      input[type="submit"] {
        display: block;
        margin-top: 20px;
        padding: 10px;
        font-size: 16px;
        border: none;
        border-radius: 4px;
        background-color: #4CAF50;
        color: #fff;
        cursor: pointer;
      }
      p.result {
        margin-top: 20px;
        padding: 10px;
        font-size: 20px;
        font-weight: bold;
        text-align: center;
        border-radius: 4px;
      }
      p.result.long {
        background-color: #f44336;
        color: #fff;
      }
      p.result.short {
        background-color: #4CAF50;
        color: #fff;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <h1>Input Length Checker</h1>
      <% if @result.present? %>
        <p class="result <%= @result %>">Your input is <%= @result %></p>
      <% end %>
      <form autocomplete="off">
        <label for="input">Input:</label>
        <input type="text" name="input" id="input" placeholder="Enter a word">
        <input type="submit" value="Check">
      </form>
    </div>
  </body>
</html>
